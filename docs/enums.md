| Enum (miejsce w schemie)                                                                       | Dozwolone wartości                                                  | Opis                                                                                                                                                                                                                                                                      |
| ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `TruthValue` (`common.schema.json#/$defs/TruthValue`)                                          | `T`, `F`, `U`                                                       | Wartość prawdziwości faktu: **T**=prawda, **F**=fałsz, **U**=nieznane (open-world).                                                                                                                                                                                       |
| `TruthDomain` (elementy, `common.schema.json#/$defs/TruthDomain`)                              | `T`, `F`, `U`                                                       | Dziedzina klastra prawdziwości (podzbiór `T/F/U`, min. 2).                                                                                                                                                                                                                |
| `Fact.status` (`fact.schema.json#/properties/status`)                                          | `observed`, `inferred_candidate`, `proved`, `rejected`, `retracted` | Status faktu w pipeline: **observed**=wyekstrahowany z tekstu, **inferred_candidate**=wnioskowany przez model, **proved**=udowodniony przez walidator, **rejected**=odrzucony (sprzeczność/kontrprzykład), **retracted**=wycofany (np. po korekcie lub przeterminowaniu). |
| `Rule.language` (`rule.schema.json#/properties/language`)                                      | `horn_naf_stratified`                                               | Dialekt reguł: klauzule Horn z negation-as-failure i wymogiem stratyfikacji (deterministyczne dowodzenie).                                                                                                                                                                |
| `Rule.body[].literal_type` (`rule.schema.json#/properties/body/items/properties/literal_type`) | `pos`, `naf`                                                        | Typ literału w ciele reguły: **pos**=pozytywny (predykat musi być dowodliwy), **naf**=NAF (predykat ma być *niedowodliwy*).                                                                                                                                               |
